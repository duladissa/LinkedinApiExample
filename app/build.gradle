apply plugin: 'com.android.application'

android {
    compileSdkVersion 21
    buildToolsVersion "21.1.2"

    defaultConfig {
        applicationId "com.litedreamz.linkedinapisample"
        minSdkVersion 10
        targetSdkVersion 21
        versionCode 1
        versionName "1.0"
    }

    productFlavors {
        dev {
            applicationId "com.litedreamz.linkedinapisample.dev"
            versionCode = 1
        }

        prod {
            applicationId "com.litedreamz.linkedinapisample.prod"
            versionCode = 1
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:21.0.3'
    compile 'org.scribe:scribe:1.3.+'
    compile 'com.google.code.gson:gson:2.3.1'
}
